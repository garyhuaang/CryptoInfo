const config = {
  KlineDataUrl:
    'http://localhost:3001/api/klines?symbol={symbol}&resolution={resolution}&from={from}&to={to}',
  ExchangeInfoUrl: 'http://localhost:3001/api/exchangeinfo',
  TickerDataUrl: 'http://localhost:3001/api/ticker',
}

export default config
